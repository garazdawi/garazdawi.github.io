.TH script 4 "sasl 4.3.1" "Ericsson AB" "Erlang Module Definition"
.SH NAME
script \- Boot script
.SH DESCRIPTION
.PP
The \fIboot script\fR describes how the Erlang runtime system is started. It contains instructions for which code to load, and which processes and applications to start.

.PP
Command \fIerl -boot Name\fR starts the system with a boot file called \fIName.boot\fR, which is generated from the \fIName.script\fR file, using \fIsystools:script2boot/1\fR.

.PP
The \fI.script\fR file is generated by \fIsystools\fR from a \fI.rel\fR file and from \fI.app\fR files.

.SS "File Syntax"

.PP
The boot script is stored in a file with extension \fI.script\fR. The file has the following syntax:

.IP
.nf
{script, {Name, Vsn},
 [
  {progress, loading},
  {preLoaded, [Mod1, Mod2, ...]},
  {path, [Dir1,"$ROOT/Dir",...]}.
  {primLoad, [Mod1, Mod2, ...]},
  ...
  {kernel_load_completed},
  {progress, loaded},
  {kernelProcess, Name, {Mod, Func, Args}},
  ...
  {apply, {Mod, Func, Args}},
  ...
  {progress, started}]}.

.fi

Name = string()
.RS 2
Defines the system name.


.RE

Vsn = string()
.RS 2
Defines the system version.


.RE

{progress, Term}
.RS 2
Sets the "progress" of the initialization program. The 
\fIinit:get_status/0\fR function returns the current value of the progress, which is \fI{InternalStatus,Term}\fR.

.RE

{path, [Dir]}
.RS 2

\fIDir\fR is a string. This argument sets the load path of the system to \fI[Dir]\fR. The load path used to load modules is obtained from the initial load path, which is given in the script file, together with any path flags that were supplied in the command-line arguments. The command-line arguments modify the path as follows:

.sp
.RS 4
.ie n \{\
\h'-04'\(bu\h'+03'\c
.\}
.el \{\
.sp -1
.IP \(bu 2.3
.\}.PP
\fI-pa Dir1 Dir2 ... DirN\fR adds the directories \fIDirN, DirN-1, ..., Dir2, Dir1\fR to the front of the initial load path.

.RE

.sp
.RS 4
.ie n \{\
\h'-04'\(bu\h'+03'\c
.\}
.el \{\
.sp -1
.IP \(bu 2.3
.\}.PP
\fI-pz Dir1 Dir2 ... DirN\fR adds the directories \fIDir1, Dir2, ..., DirN\fR to the end of the initial load path.

.RE

.sp
.RS 4
.ie n \{\
\h'-04'\(bu\h'+03'\c
.\}
.el \{\
.sp -1
.IP \(bu 2.3
.\}.PP
\fI-path Dir1 Dir2 ... DirN\fR defines a set of directories \fIDir1, Dir2, ..., DirN\fR, which replace the search path given in the script file. Directory names in the path are interpreted as follows:


.sp
.RS 4
.ie n \{\
\h'-04'\(bu\h'+03'\c
.\}
.el \{\
.sp -1
.IP \(bu 2.3
.\}.PP
Directory names starting with \fI/\fR are assumed to be absolute path names.

.RE

.sp
.RS 4
.ie n \{\
\h'-04'\(bu\h'+03'\c
.\}
.el \{\
.sp -1
.IP \(bu 2.3
.\}.PP
Directory names not starting with \fI/\fR are assumed to be relative the current working directory.

.RE

.sp
.RS 4
.ie n \{\
\h'-04'\(bu\h'+03'\c
.\}
.el \{\
.sp -1
.IP \(bu 2.3
.\}.PP
The special \fI$ROOT\fR variable can only be used in the script, not as a command-line argument. The given directory is relative the Erlang installation directory.

.RE

.RE


.RE

{primLoad, [Mod]}
.RS 2
Loads the modules 
\fI[Mod]\fR from the directories specified in \fIPath\fR. The script interpreter fetches the appropriate module by calling \fIerl_prim_loader:get_file(Mod)\fR. A fatal error that terminates the system occurs if the module cannot be located.

.RE

{kernel_load_completed}
.RS 2
Indicates that all modules that 
\fImust\fR be loaded \fIbefore\fR any processes are started are loaded. In interactive mode, all \fI{primLoad,[Mod]}\fR commands interpreted after this command are ignored, and these modules are loaded on demand. In embedded mode, \fIkernel_load_completed\fR is ignored, and all modules are loaded during system start.

.RE

{kernelProcess, Name, {Mod, Func, Args}}
.RS 2
Starts the "kernel process" 
\fIName\fR by evaluating \fIapply(Mod, Func, Args)\fR. The start function is to return \fI{ok, Pid}\fR or \fIignore\fR. The \fIinit\fR process monitors the behavior of \fIPid\fR and terminates the system if \fIPid\fR dies. Kernel processes are key components of the runtime system. Users do not normally add new kernel processes.

.RE

{apply, {Mod, Func, Args}}\&.
.RS 2
The init process evaluates 
\fIapply(Mod, Func, Args)\fR. The system terminates if this results in an error. The boot procedure hangs if this function never returns.

.RE

.PP
\fBNOTE: \fRIn an interactive system, the code loader provides demand-driven code loading, but in an embedded system the code loader loads all code immediately. The same version of \fIcode\fR is used in both cases. The code server calls \fIinit:get_argument(mode)\fR to determine if it is to run in demand mode or non-demand driven mode.


.SS "See Also"

.PP
\fIsystools\fR

